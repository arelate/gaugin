{{define "css-nav"}}
    <style id="css-nav">
        nav {
            height: 2em;
            padding: 0.5em;
            margin-top: 1em;
            display: flex;
            justify-content: center;
            align-items: center;

        }

        nav a {
            color: var(--color);
            text-decoration: none;
            padding: 0.5em;
            border-radius: 4px;
            font-weight: bold;
        }

        nav .current {
            background-color: var(--color);
            color: var(--highlight-color);
        }
    </style>
{{end}}