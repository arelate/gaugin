{{define "nav"}}
    <nav>
        <a class="{{if eq . "updates"}}{{`current`}}{{end}}" accesskey="u" href="/updates"
           rel="prefetch prerender">{{template "svg-icon" "updates"}}&nbsp;Updates</a>
        <a class="{{if eq . "filter-products"}}{{`current`}}{{end}}" accesskey="p" href="/products"
           rel="prefetch prerender">{{template "svg-icon" "stack"}}&nbsp;Products</a>
    </nav>
{{end}}