{{define "nav"}}
    {{template "css-nav"}}
    <nav>
        <a class="details {{if eq . "details"}}{{`current`}}{{end}}" href="/account">Account</a>
        <a class="store-products {{if eq . "store-products"}}{{`current`}}{{end}}" href="/store">Store</a>
        <a class="search {{if eq . "search"}}{{`current`}}{{end}}" href="/search">Search</a>
    </nav>
{{end}}