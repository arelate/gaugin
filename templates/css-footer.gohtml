{{define "css-footer"}}
    <style id="css-footer">
        footer {
            display: flex;
            justify-content: center;
            font-size: 0.75em;
            padding: 5em 0 1em 0;
            color: var(--subtle-color);
        }

        footer a {
            color: inherit;
            text-decoration: none;
            font-weight: bold;
        }
    </style>
{{end}}