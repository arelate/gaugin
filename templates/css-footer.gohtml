{{define "css-footer"}}
    <style id="css-footer">
        footer {
            position: absolute;
            bottom: 0;
            width: 100%;
            display: flex;
            justify-content: center;
            font-size: 0.75em;
            color: var(--subtle-color);
        }

        footer a {
            color: inherit;
            text-decoration: none;
            font-weight: bold;
        }
    </style>
{{end}}